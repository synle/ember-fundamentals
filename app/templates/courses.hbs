<div><h2>courses.list</h2></div>
<div>
    <ul>
        {{#each model.courses as |course|}}
            <li>
            {{link-to course.title 'courses.show' course.id}}
            </li>
        {{/each}}
    </ul>
</div>


{{outlet}}
